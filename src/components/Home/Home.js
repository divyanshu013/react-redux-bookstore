import React from 'react';
import { Link } from 'react-router';

const Home = () => (
  <div>
    This is the home component
    <button>
      Learn more
    </button>
  </div>
);

export default Home;
